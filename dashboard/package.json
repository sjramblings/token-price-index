{
  "name": "token-price-index-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "mkdir -p public && rm -rf public/data && cp -r ../data public/data",
    "dev": "vite",
    "prebuild": "mkdir -p public && rm -rf public/data && cp -r ../data public/data",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-router-dom": "^6.27.0",
    "recharts": "^2.13.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "bun-types": "^1.3.14",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
